#!/usr/bin/env python3
"""Legacy styled rulebook PDF generator for The Quiet Vale.

This script used an older hardcoded rules draft. It is kept only as a clear
compatibility marker so old notes or shell history fail safely instead of
regenerating outdated playtest rules.

Use `scripts/export_playtester_rulebook_pdf.py` for the current online
playtester PDF, or `scripts/export_styled_rulebook_from_docx.py` when starting
from a current production DOCX.
"""

raise SystemExit(
    "This legacy rulebook exporter is disabled. Use "
    "scripts/export_playtester_rulebook_pdf.py or "
    "scripts/export_styled_rulebook_from_docx.py instead."
)
